@model IEnumerable<PasportDetails.Models.PDetails>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PHolderName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PPNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PPExp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PPOCode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PHolderName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PPNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PPExp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PPOCode)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
